// Re-exporting from framer-motion to allow easier mocking if needed
import {
  motion,
  AnimatePresence,
  useInView,
  useScroll,
  useTransform,
} from "framer-motion";

export { motion, AnimatePresence, useInView, useScroll, useTransform };